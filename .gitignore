# RAG 向量索引（本地生成，不追蹤）
.rag/index/

# 護欄紀錄
.guardrail/

# Agent 狀態（本地）
.agent-events/
.agent-lock.yaml

# Autopilot 狀態（跨 session 續接，本地生成）
.asp-autopilot-state.json

# Session 審計 briefing（每次 session 自動生成）
.asp-session-briefing.json

# Managed deny 追蹤（TD-005：記錄 ASP 實際注入的 deny，供 reconcile 自我清除）
.asp-managed-deny.json

# settings.json reconcile 安全備份（session-audit Section 10 產生，本地還原用，勿提交）
.claude/settings.json.bak

# Gate 狀態（session 內追蹤）
.asp-gate-state.json

# 測試結果（asp-ship 寫入，session-audit 讀取）
.asp-test-result.json

# Bypass log（v3.5 新增，append-only skip 事件）
.asp-bypass-log.json

# 測試覆蓋率
.coverage
.coverage.*
htmlcov/

# Session 紀錄（本地）
docs/session-log.md

# 安裝備份
CLAUDE.md.pre-asp
CLAUDE.md.pre-upgrade
Makefile.pre-asp-upgrade

# 安裝暫存
.asp-tmp/

# 暫存
tmp/
*.out
*.zip

# 環境變數
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
.asp-audit-baseline.json

# Python
__pycache__/
*.pyc
.pytest_cache/

# Bypass log NDJSON (Iron Rule B append-only — local audit trail)
.asp-bypass-log.ndjson

# Bypass-log high-water-mark（TD-002：append-only 截斷偵測基準，本地狀態）
.asp-bypass-log.hwm

# Generated diagram output (make diagram)
docs/architecture.html
.asp-daily-report.md

# 技術債清單 — 2026-06-08

來源：subagent independent review（今日 merge PR #4/#8/#9 後）

---

## TD-001 — session-audit.sh 區段編號重複 [MINOR]

**位置：** `.asp/hooks/session-audit.sh` 第 278 行、第 306 行  
**問題：** PR #8 加入 self-clearing 邏輯時未更新後續編號，現有兩個「# 10.」  
**修法：** 將輸出摘要區段改為「# 11. 輸出摘要」  
**風險：** 無功能影響，僅維護性問題

---

## TD-002 — bypass-log 截斷偵測邏輯語義錯誤 [IMPORTANT]

**位置：** `.asp/hooks/session-audit.sh` 第 56-67 行（Iron Rule B）  
**問題：** `LINE_COUNT`（bypass 事件行數）與 `GIT_LINE_COUNT`（git commit 次數）是不同單位，比較無法可靠偵測截斷  
**修法：**
```bash
# 改為從 git 取得上次提交時的行數
PREV_LINE_COUNT=$(git show HEAD:"${NDJSON_LOG}" 2>/dev/null | wc -l || echo 0)
if [ "${LINE_COUNT}" -lt "${PREV_LINE_COUNT}" ]; then ...
```
**風險：** Iron Rule B 偵測效果低於預期，截斷攻擊可能未被偵測

---

## TD-003 — SPEC-004 編號衝突 [IMPORTANT]

**位置：** `docs/specs/`  
**問題：** 存在兩個 SPEC-004：
- `SPEC-004-benchmarks.md`
- `SPEC-004-multi-agent-worktree-isolation.md`

**修法：** 將 `SPEC-004-benchmarks.md` 重新編號為 `SPEC-007`，更新所有引用  
**風險：** audit 工具或 session-audit.sh 做「SPEC-004 是否存在」判斷時可能結果不一致

---

## TD-004 — Draft 偵測 regex 過寬，可能誤報 [IMPORTANT]

**位置：** `.asp/hooks/session-audit.sh` 第 152 行  
**問題：** `` `Draft` `` backtick 模式會匹配 ADR 內文說明段落（如「`Draft`（初稿）→ `FIRM` → `Accepted`」），不只匹配表格狀態欄  
**修法：** 改為精確匹配表格欄位：
```bash
grep -qiE "\|\s*\`Draft\`\s*\|" "$adr_file"
```
**風險：** 若未來 Accepted ADR 內含流程說明樣板文字，可能被誤判為 Draft 並觸發 commit deny

---

## TD-005 — managed deny 可能誤刪使用者手動設定 [IMPORTANT]

**位置：** `.asp/hooks/session-audit.sh` Section 10 自清邏輯（第 284-303 行）  
**問題：** reconcile 邏輯依固定字串移除 MANAGED_DENY，若使用者手動加入相同的 `Bash(git commit *)` deny（出於其他原因），下次 session 無 Draft ADR 時會被靜默移除  
**修法：** 在 settings.json 中為 ASP 注入的 deny 加識別 comment，或改用獨立欄位 `asp_managed_deny`，reconcile 只清除自己注入的條目  
**風險：** 使用者手動設定的 deny 被靜默移除，使用者毫不知情

---

## TD-006 — ADR-009 Trial 尚未完成（N=1，需 ≥3）[追蹤項]

**位置：** `docs/adr/ADR-009-asp-plan-step5-auto-subagent-gate.md` 後續追蹤  
**問題：** ADR-009 因 audit BLOCKER 緊急升 Accepted，但 Trial 前置條件（≥3 distinct plans）尚未完成，目前 N=1  
**下一步：** 每次有 ADR/SPEC 異動的 plan，在 ADR-009 後續追蹤記錄一筆（catch/noise/漏報），累積到 N=3 後進行 Trial 結果評估  
**風險：** P2 auto-spawn gate 效果未經足夠驗證即升 Accepted

---

## TD-007 — SPEC-006 尚未實作（ADR-009 硬依賴）[追蹤項]

**位置：** `docs/specs/SPEC-006-asp-plan-step5-auto-gate.md`（文件已存在，但 skill 未實作）  
**問題：** `asp-plan.md` Step 5 auto G1/G2 spawn、`asp-ship.md` Step 9.6、trigger glob 命令均尚未寫入對應 skill 檔案  
**前置條件：** SPEC-006 通過 G2 審查後才能動 `.claude/skills/asp/*.md`  
**下一步：** 走 asp-plan flow，SPEC-006 → G2 → 實作 → G3/G4

---

## 優先順序建議

| 優先 | TD | 理由 |
|------|----|------|
| 立刻 | TD-004 | 可能誤觸 commit deny，影響日常開發 |
| 下次 session | TD-002 | Iron Rule B 偵測效果問題 |
| 下次 session | TD-003 | SPEC-004 編號衝突影響工具正確性 |
| 下次 session | TD-005 | 使用者手動 deny 被靜默移除 |
| 持續追蹤 | TD-006 | Trial N=1，每次 plan 記錄一筆 |
| 排入 backlog | TD-007 | SPEC-006 實作（需走完整 asp-plan 流程） |
| 最低優先 | TD-001 | 純維護性，無功能影響 |

---

## 處理結果（2026-06-08 remediation session）

> 流程：每項以 TDD（red→green）修復 + `make test` 全綠 + **對抗式驗證 workflow**（4 個獨立 skeptic agent 嘗試攻破 TD-002/004/005）。對抗驗證揭露 first-pass 修法的真實漏洞，已於 round 2 修正。

| TD | 狀態 | 摘要 |
|----|------|------|
| **TD-001** | ✅ 完成 | 區段編號 `# 10. 輸出摘要` → `# 11.`（deny-sync 維持 `# 10.`）。section 編號已唯一。 |
| **TD-002** | ✅ 完成（**偏離原提案**） | 原提案「`git show HEAD` 取上次行數」**不可行**——對抗驗證證實 `.asp-bypass-log.ndjson` 是 **gitignored**，HEAD 永遠為空 → 偵測恆失效（舊碼亦然）。改用 **high-water-mark sidecar**（`.asp-bypass-log.hwm`，已 gitignore）：跨 session 記錄行數峰值，掉到峰值以下即 BLOCKER。`awk NR` 計數防 trailing-newline 偏移。已在**真實 log 上驗證**會觸發。test：7/7。 |
| **TD-003** | ✅ 完成（選項 A） | 釐清 `SPEC-004-benchmarks.md` 實為 SPEC-004 worktree 的**效能交付子文件**，非撞號獨立 SPEC。`git mv` → `SPEC-004-multi-agent-worktree-isolation-benchmarks.md`，更新 worktree spec（×2）+ CHANGELOG 引用 + 檔頭加交付物註記。**未**採原提案「升 SPEC-007」（會把交付物誤升為獨立 spec）。 |
| **TD-004** | ✅ 完成（**round 2 強化**） | round 1 收窄為 backtick 表格欄位；對抗驗證發現**漏報破口**：repo 內 SPEC 用**無 backtick** 狀態格（`\| **狀態** \| Accepted \|`），且欄內常加 `（待確認）` 註解——這些格式的 Draft ADR 會**繞過 commit BLOCKER**。round 2 改為**錨定 `**狀態**`/`Status` 標籤格 + 值格內任意位置 `\bDraft\b`/`\bFIRM\b`**（容 backtick/無 backtick/註解），同時仍排除內文 legend 與 `驗證摘要` 格誤報。test：15/15（含 no-backtick/annotated 向量）。 |
| **TD-005** | ✅ 完成（**round 2 強化** + 殘留待決） | round 1 sidecar（`.asp-managed-deny.json`）記錄「ASP 實際注入」項，只自清自己注入的，使用者既有手動 deny 不被刪。round 2 修正對抗驗證發現的：(1) **我引入的 settings.json 損毀風險**——加「非空 + 合法 JSON + 備份成功才 mv」效驗閘；(2) **遺失 sidecar 永久卡死**——sidecar gitignored 但 settings.json tracked，換機後 ASP 注入的 deny 無法自清 → 加 **orphan WARNING**（不自動刪/不靜默留，讓使用者決）。test：10/10。 |
| **TD-006** | ✅ 完成（追蹤） | ADR-009 後續追蹤加 Trial 日誌：本 session 未走 asp-plan Step 5 且 auto-gate 未實作 → **N 維持 1**；明載 Trial 進度**硬依賴 TD-007（SPEC-006 落地）**。未捏造資料點。 |
| **TD-007** | ✅ 確認（不實作） | SPEC-006 文件存在但 skill 未實作；ADR-009 L117 明禁未過 G2 前動 `.claude/skills/asp/*.md`。維持 backlog，需走 asp-plan → G2 → 實作 → G3/G4。 |

### 已知殘留（documented residuals，建議後續處理）

1. **TD-005 架構性缺口 → 已開 [ADR-011](adr/ADR-011-isolate-asp-dynamic-deny-in-settings-local-json.md)（Draft）**：ASP 動態 deny 寫入 **git-tracked** 的 `settings.json`，而 ownership 紀錄在 **gitignored** sidecar——兩者會 desync（換機/`git clean`），且「使用者在 ASP 擁有期間加入相同字串」無法事後辨識。**完整解法**：把 ASP 動態 deny 改寫到 **gitignored 的 `.claude/settings.local.json`**（已查證 Claude Code 採 deny-first / scope-merge，見 `.asp-fact-check.md` FC-001）。ADR-011 已記錄此決策（Draft，待人類審核）；本 session 以 orphan WARNING + 效驗閘緩解，架構遷移待 ADR-011 Accepted 後走 SPEC→G2→實作。
2. **TD-005 低嚴重度**：reconcile 用 `unique` 會排序 deny——若使用者 deny 非排序則會一次性重排（本 repo 已排序故無影響）。settings.local.json 遷移可一併解決。
3. **TD-002 殘留**：等量「替換」（行數不變但內容換掉）與 `.hwm` sidecar 自身被竄改，line-count heuristic 無法偵測；需 per-entry hash-chain。已於程式碼註解標明，屬 audit 完整性的進階主題。

### Commit 阻擋狀態（本 session 未 commit）

- 🔒 **ADR-010 仍 Draft** → 動態 deny 阻擋 `git commit`（鐵則，正確行為）。本 session 所有修改**已就緒但未提交**。
- 🔒 **Iron Rule A**：`session-audit.sh` 已修改但未 staged → 下次 session 會報 hash mismatch BLOCKER（預期，commit/stage 後解除）。
- 解除路徑（人類決定）：審核 ADR-010 升 Accepted（或 `make asp-unlock-commit`）→ review 本批 diff → commit。`settings.json` 內的 `Bash(git commit *)` 為 ADR-010 Draft 的暫態注入，**勿一併 commit**（ADR-010 解除後會自清）。
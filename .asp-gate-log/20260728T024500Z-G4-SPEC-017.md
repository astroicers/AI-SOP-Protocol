---
gate: G4
target_id: SPEC-017
target_path: docs/specs/SPEC-017-worktree-aware-advisory-checks.md
trigger_commit: 5c5b570（base main；實作於分支 asp/spec-017-impl，head hash＝本 PR 首 commit）
trigger_diff_command: "git diff main --name-status"
spawn_timestamp_utc: 20260728T024500Z
subagent_type: main-agent TDD + workflow wf_90ca649d-75a 對抗式審查（G5 前哨）
subagent_model: fable-5
result: PASS
findings_count: 9
---

# G4 Gate Report — SPEC-017 Implementation

## 機械證據

| 檢查 | 證據 |
|---|---|
| 專項測試綠 | resolve 20/20→修入 review findings 後含 SH-01 新斷言重跑（見下）；race 6/6；shipgate 回歸 7/7 |
| `make test` | bash 54 檔全過、pytest 16 過、RC=0（T8：Iron Rule A/B、A19.1 list 回歸全綠） |
| `make lint` | RC=0；F5 後 lib 已入 shellcheck 清單 |
| E2E 實景 | 本 worktree 為 stdin.cwd、主樹為 anchor 跑真 hook：A19.1 cry-wolf 消除、briefing `worktree_resolution:"worktree"` |
| Done When 9 | session-audit 檔首 + claude-md-reference.md 兩處 grep 命中 |
| scope | 改動檔全在 SPEC Traceability 清單 + F1-F5/SH-01 修復檔 |

## 實作決策記錄

- **lib 載入點**（SPEC 未指明）：hook 自身位置（`BASH_SOURCE`）→ PROJECT_DIR → `~/.claude/asp/scripts/lib/` fallback 鏈；全缺→退錨 fail-open / ship-gate stub 守 anchor。
- **歧義操作化**：`suppressed_a18` ⇔ stdin.cwd 非空 ∧ 其 git top 缺或 ≠ anchor ∧ helper 回 anchor；正常主樹與無 stdin 維持現行。
- **自引入問題即時修復**：新測試檔逐字 tech-debt 標記造成 A8.3 假陽性 → 拆字串修除（比照 test_tech_debt_scan_exclusions.sh:34）。

## 對抗式審查（workflow `wf_90ca649d-75a`，12 agents 唯讀）結果與處置

9 findings 全 confirmed（0 誤報）+ 23 passes（含三錨 INV-1 抽查、Done When 1-10 逐條、血緣守衛、set -u/pipefail 全路徑）：

| # | Sev | Finding | 處置 |
|---|-----|---|---|
| TASK-INPUT-001 | HIGH | anchor-security 維度 agent 收到字面 "NaN" prompt、該輪空跑（orchestration 異常，非本實作缺陷） | **補跑**：獨立唯讀 subagent 重審三錨安全維度（結果見下方補記） |
| F1 | HIGH | 合併檔名 `G3-G4` 違反 SPEC-006 gate log pattern → test_auto_gate_log_write 紅 | 拆為本檔 + G3 檔，檔名合規，重跑 make test 綠 |
| F2 | MED | lib 在 Iron Rule A 驗證**前**被 source → 被竄改 lib 可先於檢查執行、弄啞對自身偵測 | source 區塊移至 Iron Rule A **之後**；Iron Rule A 對 lib 出 BLOCKER 時**拒 source** 退錨 |
| F3 | MED | A16 gate 讀 $WT profile 但 compile 錨主樹（跨樹混用、越 SPEC 範圍） | A16 gate 改讀 `${PROJECT_DIR}/.ai_profile`（A16 全錨主樹；A1 維持 worktree-local） |
| SH-01 | LOW | CLAUDE_PROJECT_DIR 尾斜線 → 主樹誤判 worktree、A19.1 被誤抑制 | lib + session-audit anchor 去尾斜線正規化；新增 T3b/T3c 斷言 |
| SH-02 | LOW | anchor 含 symlink → 恆退錨（保守方向，僅失去感知、不誤放） | 列已知限制（lib 註解）；不修（修復違反 _abs_common_dir「逐字取自 #72」鐵律） |
| SH-03 | LOW | 同 F3（重複面向） | 併 F3 處置 |
| F4 | LOW | race 測試保留一個舊 harness worktree 案例（字面違反 DW3） | 該案主體＝「anchor 本身即 worktree」分支回歸、非模擬 worktree session → 註記後保留（合法豁免） |
| F5 | LOW | 新 lib 未入 make lint shellcheck 清單 | Makefile.inc 清單加入 lib |

## 補記：anchor-security 補跑結果（獨立唯讀 subagent，審修復後最終狀態）

**verdict: PASS_WITH_NOTES** — 9 passes + 3 LOW notes（全屬可接受殘留，不修碼）：

- **Passes**：INV-1 三錨零 `$WT`（grep 全檔：$WT 僅 advisory；三錨區塊零命中）；A19.1 list/Iron Rule B 回歸測試全綠（iron_a 8/8、iron_b truncation 7/7、hashchain 15/15）；血緣守衛抗惡意 stdin 實測（`-`/`--exec-path=/evil`/換行/空白/引號注入 5 種 cwd 全部安全退錨、無檔案生成）；F2 修復屬實（source 於 Iron Rule A 後 + BLOCKER 拒 source）；ship-gate stub 降級 fail-safe（刪 lib 反而更保守、且觸 Iron Rule A BLOCKER）；A18 suppress 留痕充足（INFO 行+briefing 欄位，resume 不會無跡消失）。
- **L1**（LOW）：planted `.git` 指標檔與 anchor 共用 object store 時被視為合法 worktree——#72 逐字沿用之既有行為、在其信任模型內（守「無關 repo」；同 common-dir 非無關），非 SPEC-017 迴歸。
- **L2**（LOW）：lib 第三順位 HOME 安裝點不在 Iron Rule A（git-HEAD-based）覆蓋——能寫 HOME 者已越過信任邊界；真實調用下第一順位＝repo 副本＝Iron Rule A 所驗檔。可接受殘留。
- **L3**（LOW）：git env 信任假設僅記於 ship-gate 註解——已修（session-audit SPEC-017 區塊補引用一行）。

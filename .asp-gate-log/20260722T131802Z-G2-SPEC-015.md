---
gate: G2
target_id: SPEC-015
target_path: docs/specs/SPEC-015-pure-plugin-profile-standalone.md
trigger_commit: b676255
trigger_diff_command: "git diff --cached --name-status"
spawn_timestamp_utc: 20260722T131802Z
subagent_type: reality-checker
subagent_model: opus-4-8
result: PASS_WITH_WARN
findings_count: 5
---

# G2 Gate Review Report — SPEC-015

**Target:** docs/specs/SPEC-015-pure-plugin-profile-standalone.md
**Verdict (initial):** NEEDS_WORK → **resolved PASS_WITH_WARN**（findings 實作前修入，比照 SPEC-006/014 gate-log 慣例）
**Reviewer:** 獨立 read-only reality-checker（預設 skeptical），findings 經主 agent 對 code line 核實後採納。

## 核心前提通過（reviewer 逐一驗）
- asp-compile 已支援 `--asp-root`（`:30/:41`）、session-audit 呼叫端如述（`:180-184` 無傳 --asp-root）。
- **compiled 產物自足**（關鍵）：`grep '~/.claude' .asp/profiles/*.md` = 0；asp-compile `:200-235` 僅串接 profile 內文 + header；CLAUDE.md step 0 確實先讀 compiled 產物 → SPEC 核心機制成立。
- ADR-027「不 ship 下游 CLAUDE.md」立場真實且 Accepted，DP2 已正確 gate 於「須先 ADR」。

## Findings（5）與處置
| # | Finding（核實屬實） | 修正 |
|---|---|---|
| F1（BLOCKER）| DP3 選 project→plugin→home，但 dual-path（home+plugin、無 project）下 **plugin 搶走 home** → 牴觸 marketplace.json:13「dual-path profile 由 installer 提供」；S2 只測 CLAUDE_PLUGIN_ROOT 未設、未覆蓋共存 | DP3/E2 改 **project→home→plugin**；新增 **S5 gherkin + P2b + Done-When**（plugin+home→home 勝）|
| F2（missed site）| `session-audit.sh:117` Iron Rule B bypash-hash 探索同一兩分支 gap → 純 plugin tamper-evidence 靜默略過 | Side Effects + Done-When 納入 bypash-hash plugin 段 |
| F3（gap）| N1「印 INFO」錯——`:166-170` 現已是 A1 **WARNING** 且未列入 edit site | 更正 N1/S3 為「augment 既有 WARNING」+ Side Effects 列此 edit site |
| F4（under-spec）| DP1「port installer Phase 2」非直譯——commands 是自然語言檔；且 install.sh:505-524 hook 佈線不適用純 plugin | DP1 改「AI 驅動對話（AskUserQuestion）」+ 明確排除 hook 佈線步驟 |
| F5（minor）| `validate-profile.sh:86` LEVEL_FILE 相對 CWD——但不 increment ERRORS、僅 cosmetic | 記錄，非 gate blocker |

## 結論
初判 NEEDS_WORK 的 4 個實質 finding（+1 minor）全數屬實、實作前修入 SPEC v2（探索順序更正 + 兩個 missed Iron-Rule 站補入 + DP1 重寫）。→ **PASS_WITH_WARN**：可進人類核准，實作輪據 SPEC v2 TDD。

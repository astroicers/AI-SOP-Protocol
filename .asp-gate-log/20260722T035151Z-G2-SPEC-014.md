---
gate: G2
target_id: SPEC-014
target_path: docs/specs/SPEC-014-asp-marketplace-plugin-distribution-implementation.md
trigger_commit: 963c69c
trigger_diff_command: "git diff --cached --name-status"
spawn_timestamp_utc: 20260722T035151Z
subagent_type: reality-checker
subagent_model: opus-4-8
result: PASS_WITH_WARN
findings_count: 7
---

# G2 Gate Review Report — SPEC-014

**Target:** docs/specs/SPEC-014-asp-marketplace-plugin-distribution-implementation.md
**Verdict (initial):** NEEDS_WORK → **resolved to PASS_WITH_WARN**（7 findings 全數於實作前修入 SPEC，比照 SPEC-006 gate-log 慣例）
**Reviewer:** 獨立 read-only `reality-checker` subagent（預設 skeptical），findings 經主 agent 逐條對照實際 code line 核實後採納。

## 背景
SPEC-014 為 ADR-021 marketplace 分發的實作規格。G2 review 對「plugin 資源解析已解」的核心宣稱做對抗式檢驗。

## Findings（7）與處置

| # | Finding（核實屬實） | 修正（SPEC 版本 2，enforcement-first） |
|---|---|---|
| F1 | `session-audit.sh:56-64` Iron Rule A CRITICAL_FILE 檢查是 `git -C PROJECT_DIR show HEAD:.asp/…` 比對，與 line 117/180 discovery 迴圈無關；plugin-consumer 專案不 track `.asp/` → 該檢查靜默 inert，原 SPEC 未察 | E3 明載此 inert 為**刻意 design decision**（plugin loader git-SHA 承載完整性）；**不**試圖在 consumer 重建 Iron Rule A |
| F2 | 「Iron Rule A HWM hash 重基線」概念錯誤——HWM 是 **Iron Rule B**（bypass-log 行數，ADR-019 / memory `project_ironb_gitignored`）；Iron Rule A 是 live git-diff、無 stored hash 可 rebaseline | 移除該 Done-When/Side-Effect；正確區分 A（git-based，不動）與 B（bypass-hash，可選 plugin-root 段） |
| F3 | `clean-allow-list.sh:21` `DENIED_FILE=${PROJECT_DIR}/.asp/hooks/denied-commands.json` **無 fallback 鏈**（靠腳本內建 default），非原 SPEC 宣稱的 PROJECT→HOME 鏈 | E6 + 不變式表更正現狀；enforcement-first 下靠內建 default，不需改 |
| F4 | `session-audit.sh:337` `inbox-ingest.sh` PROJECT_DIR-only、原 SPEC 未提；plugin+orchestrator 下 A15.1 held 警告靜默略過 | E6 + 不變式表納入；判定 out-of-scope 優雅降級（非 fatal） |
| F5（主）| 「2 for-loop 小改」只讓**強制力 hooks** 可用；profile/level/template 內容因 `asp-compile.sh:41` 自有 root 解析（無 plugin 分支）+ `session-audit.sh:184` 未帶 `--asp-root` + CLAUDE.md 硬編 `~/.claude/asp` 而**無法** plugin-root 解析。原 Expected Output 過度宣稱 | **範圍收窄 enforcement-first**（人類 2026-07-22 拍板）：SPEC 只管強制力層；profile 內容 dual-path 靠 installer、standalone 化列 follow-up。核心 deny+閘 POC-1 證**零 `.asp/` 依賴** |
| F6 | E2「不裝進自身」只解自我安裝；ADR-021 選項 C dual-path 明確設計 installer+plugin **共存** → 同專案雙擊；原 sentinel 標 optional | E2/S5/B3：冪等 sentinel 改為**必要**；新增共存 harness Done-When |
| F7 | Done-When item 2 綁 CI-可驗（harness）與 human-only（互動 `/plugin`）於同一框，無法乾淨閘控 | 拆為 2a（CI-gateable harness）+ 2b（human sign-off，明確排除自動閘） |

## Non-issues（已檢查無虞）
- 7 必填欄位齊、Traceability 為 pre-impl placeholder（正常）。
- 術語：無 CONTEXT.md 避免使用同義詞。
- Gherkin：4 場景各 ≥1 `Then`、含負向 + 邊界。
- E5（測試退役反轉）論證扎實、證據充分。
- `bypass-hash.sh`（session-audit.sh:117）自身無巢狀資源解析問題。

## 結論
初判 NEEDS_WORK 的 7 findings 全數屬實（主 agent 已對 `session-audit.sh:56-64/337`、`clean-allow-list.sh:21`、`asp-compile.sh:41`、`session-audit.sh:184` 逐一核實），並於**實作前**修入 SPEC v2（enforcement-first 收窄 + 機制更正 + 必要 sentinel + Done-When 拆分）。→ **PASS_WITH_WARN**：可進入人類核准，實作輪據 SPEC v2 TDD。

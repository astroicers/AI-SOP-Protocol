---
gate: G1
target_id: ADR-025
target_path: docs/adr/ADR-025-quantified-scope-discipline-advisory.md
trigger_commit: 4cdd7dd
trigger_diff_command: "git diff --cached --name-status"
spawn_timestamp_utc: 20260625T020331Z
subagent_type: general-purpose
subagent_model: opus-4-8
result: PASS_WITH_WARN
findings_count: 5
---

# G1 Review — ADR-025

## Verdict: GATE_PASS_WITH_CONCERNS（PASS_WITH_WARN，不阻擋）

獨立 read-only subagent 審查 ADR-025（scope 紀律量化 advisory，Draft）。無 high；2 med + 3 low。reviewer 實跑重現 POC（`SCOPE_DIST total=40 <=8檔=29 >8檔=11 max=142檔`，exit 0）、實讀 asp-ship.md Step 2 證實「增強既有步驟、零新層」屬實、FC-005 證實「不照搬 ≤5 二手數字」。核心評價：證據紮實、ADR-010 摩擦評估誠實、鐵則零違反；價值屬邊際但成本同樣極小、已誠實揭露重疊度。

| # | Severity | 維度 | Finding | 處置 |
|---|----------|------|---------|------|
| 1 | MED | traceability | ④ 落點由 roadmap 的 asp-impact 改為 asp-ship Step 2（決策更好），但 roadmap 未回寫 | **本批已修**：roadmap ④ 列 + §④ 回寫落點演進；ADR 決策段加「落點演進（修正 roadmap）」 |
| 2 | MED | 實作語意 | ADR 用 `git diff --cached --stat`，既有 Step 2 用 `git diff --stat`（無 --cached）；--cached 更正確但非「零改動」 | **本批已修**：ADR 決策段明標「量測對象=staged（--cached）、小幅增強非零改動、SPEC 階段對齊 Step 2」 |
| 3 | LOW | 閾值穩健 | 閾值 8 落在 7/9 commit 數的天然空隙（無 commit 恰 8 檔），未點出 | **本批已修**：ADR 決策段加「落在 7/9 空隙、邊界穩健」 |
| 4 | LOW | 外推性 | 閾值校準只用 ASP 自身 40 commit，他用專案應自校 | **本批已修**：ADR 決策段加「ASP 校準值、他用專案應自校」 |
| 5 | LOW | 指標時機 | 成功指標多為「實作時」驗證，Draft 階段無法現驗 | 接受（advisory-only、Accepted 後才實作之合理現象；G5 reality-check 時回補） |

## 逐項

- **決策↔證據**：POC 數字三方一致且可重現；閾值 >8 與「advisory 不硬 gate」被 max=142 支持。
- **ADR-010 摩擦評估**：誠實——實讀 asp-ship Step 2 確有質性 scope 檢查；④ 是增強同一步驟、零新層；選項 A（新 hook/script）被正確駁回為重疊。
- **不照搬二手**：FC-005 證實 ≤5 一手未載；ADR 借精神不照搬，閾值 8 為 POC 校準。
- **結構/編號/鐵則**：九章齊、ADR-025 未撞、Draft + Evidence 留白待人類、POC 限 spike、無 AI 自升。
- **是否該不做**：值得做（質性→量化、成本極小），邊際性質已誠實揭露。

reviewer: general-purpose subagent (opus-4-8), read-only；trigger: asp-plan Step 5.5 auto-gate (ADR-009)；MED-1/MED-2/LOW-1/LOW-2 已於同批修正

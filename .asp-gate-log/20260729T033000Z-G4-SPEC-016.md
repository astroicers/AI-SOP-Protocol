---
gate: G4
target_id: SPEC-016
target_path: docs/specs/SPEC-016-pretooluse-git-guardrails-block-local-destructive-git.md
trigger_commit: 388d1aa（base main；實作於分支 asp/spec-016-impl）
trigger_diff_command: "git diff main --name-status"
spawn_timestamp_utc: 20260729T033000Z
subagent_type: main-agent TDD + workflow 對抗式審查（G5 前哨）
subagent_model: fable-5
result: PASS
findings_count: 待審查回填
---

# G4 Gate Report — SPEC-016 Implementation

## 機械證據

| 檢查 | 證據 |
|---|---|
| 專項測試綠 | `test_pretooluse_git_guardrails.sh` **80/80** |
| 連動測試綠 | `test_iron_rule_a_coverage` 10/10、`test_rule_registry` 27/27（含 +3 GIT-GUARD 顯式斷言）、`test_plugin_manifest` 18/18（refs≥4 + hooks[1] 綁定） |
| `make test` | bash **55** 檔全過（+1＝新 hook 測試）、pytest 16、RC=0 |
| `make lint` | RC=0；新 hook 已入 shellcheck 清單（手動先驗乾淨，比照 SPEC-017 F5 教訓） |
| E2E 實景 | 真 hook 實測：`reset --hard`→deny／`clean -nfd`→defer／`git -C /x reset --hard`→deny（全域選項跳過）／`ASP_GIT_OK=1`→defer+bypass 遙測 |
| scope | 改動 10 檔全在 SPEC Traceability 清單（新 hook + 4 wire/lint + 4 測試 + SPEC/ADR-030/FC-013） |

## 實作核心（M0/M1 引擎）

- **M0**：`_split_segments`（引號感知字元狀態機，`;&|`+換行 分段）+ `_load_argv`（引號感知 tokenize），純 bash 無外部相依。
- **M1**：9 個子命令謂詞（reset/clean/branch/checkout/restore/switch/stash/worktree/rm），helper `_arg_has`（case-sensitive token）／`_bundle_has`（短旗標捆綁字母集）／`_positionals`。全域選項跳過含 `-C`/`-c`/`--git-dir` 空白分隔形吃參數、`--exec-path` 不吃（FC-013）。
- **踩雷修正**：`local s="$1" n=${#s}` 同行宣告在 `set -u` 下對 `${#s}` 求值時 s 尚未可見 → 拆行（首次跑全 deny 案放行的根因）。

## 補記：G5 前哨對抗式審查（workflow `wf_adb03666-9fb`，6 agents 唯讀）

三維度（謂詞 over/under-block／shell 注入面／SPEC 符合度）+ 逐 finding 反駁驗證。**34 passes + 3 findings 全 confirmed（0 空跑）**。唯讀鐵則守住（真實檔未動、無 pwned）。

| # | Sev | Finding | 處置（修入） |
|---|-----|---|---|
| **UB-01** | **HIGH** | switch/checkout 的 force 只用精確 token、缺捆綁感知 → `git switch -qf`/`checkout -qf` 漏擋（/tmp fixture 實測**真的銷毀未提交變更**）；80/80 綠仍漏因 N4/N7 只測精確式 | 新增順序感知 `_bundle_has_flag`（掃到需參數旗標 -b/-B/-c/-C 即停，其後為參數）；switch/checkout force 判定改用之。手動驗：`-qf`→deny、`-cf`/`-Bf`（建分支，f 為參數）→defer。補測試 N4f/g、N7c/d/e + 回歸 P13a/b |
| **GG-SEC-01** | MED | 純 bash 逐字元 tokenizer O(n²)、無長度上限 → 超長 command 使 hook 停滯數十秒 → timeout→fail-open 繞過向量 | 加 O(1) 長度 guard（`${#COMMAND} ≤ 8192` 否則靜默 defer）；真實 git 命令遠短於此，超長屬可接受 fail-open。補測試 GG-SEC-01 |
| **OB-01** | LOW | `_pred_stash` 未把 `--` 當選項終止符 → `git stash -- drop`（對名為 drop 的檔安全 push）誤擋 | stash 迴圈遇 `--` 即 return 1（其後 pathspec）。補測試 P13c/d |

**spec-compliance 維度：18 passes、0 findings**（M0/M1 逐條、輸出規範、五處 wire 連動、測試矩陣覆蓋皆符合 SPEC v2、無偷渡）。

**修復後複驗**：test_pretooluse_git_guardrails **90/90**（+10 案）、make test 55 bash 全綠、shellcheck + lint 綠。SPEC M1 表 switch/checkout 逐列補「捆綁含 f（順序感知）」消除 rule #5 通用 vs 逐列窄寫的內部矛盾（UB-01 根因）。

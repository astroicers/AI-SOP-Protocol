# ASP 外部事實查證紀錄（.asp-fact-check.md）

> 鐵則「外部事實驗證防護」：涉及第三方 API / 版本 / 法規 → 必須查證並記錄於此。

---

## FC-001 — Claude Code `settings.local.json` 的 deny 合併行為

- **日期**：2026-06-08
- **觸發**：ADR-011（將 ASP 動態 deny 隔離至 settings.local.json）的核心假設
- **查證方式**：claude-code-guide subagent 對照官方文件（code.claude.com/docs/en/settings、/permissions）
- **結論（已確認）**：
  1. `.claude/settings.local.json` 是 Claude Code 自動載入的設定 scope。✅
  2. `permissions.deny` / `permissions.allow` **跨 scope 合併**；評估順序 **deny → ask → allow**，**任一 scope 的 deny 即阻擋**（deny-first，與檔案優先序無關）。✅
  3. 優先序：Managed > Command-line > **Local (settings.local.json)** > Project (settings.json) > User。✅
  4. `settings.local.json` **預設 gitignored**，定位為本地 / 不提交的 per-project override。✅
  5. Hook 寫入 settings.local.json 的 deny 同 session 自動 reload 生效，並持續至下次 session。✅
- **對 ADR-011 的意涵**：把 ASP 動態 deny 寫到 settings.local.json，其強制力與寫在 settings.json **等價**（deny-first、scope-agnostic），同時獲得 gitignored + 本地隔離 → 解決 TD-005 殘留。
- **再驗證條件**：Claude Code 若變更 settings 載入或 deny-merge 語意，FC-001 失效，須重審 ADR-011。

---

## FC-002 — Claude Code PreToolUse hook 介面（deny 機制 / input schema）

- **日期**：2026-06-12
- **觸發**：ADR-020 / SPEC-013（PreToolUse commit 閘）的核心實作假設
- **查證方式**：claude-code-guide subagent 對照官方文件（code.claude.com/docs/en/hooks）
- **結論（已確認）**：
  1. **settings.json 配置**：`hooks.PreToolUse[]`，每項含 `matcher`（工具名，regex/字串，`"Bash"` 匹配 Bash 工具）+ `hooks[]`（`type:"command"` + `command`）。✅
  2. **hook stdin JSON**：含 `tool_name`（如 `"Bash"`）、`tool_input.command`（Bash 指令字串）、`hook_event_name`、`cwd` 等。✅
  3. **Deny 機制（兩法）**：
     - 方式 A（採用）：`exit 0` + stdout JSON `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"..."}}` → 阻擋並把 reason 回饋 AI。`permissionDecision` 值域：allow/deny/ask/defer。
     - 方式 B：`exit 2` + stderr → 阻擋，stderr 回饋 AI。其他 exit code = 非阻擋。✅
- **不採用 / 不依賴**：agent 提到的 settings `if:"Bash(git commit *)"` 欄位**未在採用範圍**（不確定是否穩定）→ SPEC-013 改為 `matcher:"Bash"` + **hook 腳本內自行 grep command 判斷是否 git commit**（可控、不依賴未確認欄位）。
- **對 SPEC-013 的意涵**：commit 閘用方式 A（exit 0 + permissionDecision deny），腳本內解析 `tool_input.command`，非 git commit 一律 `defer`（不干擾其他 Bash）。
- **再驗證條件**：Claude Code 若變更 PreToolUse hook 的 stdin schema 或 deny 輸出格式，FC-002 失效，須重審 SPEC-013 hook 腳本。

---

## FC-003 — Claude Code 自訂 slash 指令的子目錄命名（`commands/asp/` → `/asp:xxx`）

- **日期**：2026-06-17
- **觸發**：安裝器同步 commands bugfix 採 namespaced 子目錄 `~/.claude/commands/asp/`；對抗式審查提出 blocker 主張子目錄不產生 `/asp:` 前綴（裝完叫 `/approve-adr`）。需定第三方行為。
- **查證方式**：
  1. WebFetch 官方文件 `code.claude.com/docs/en/slash-commands`（命名表 + namespacing 段，存檔於 tool-results）。
  2. 本 session live 實證：建立 `.claude/commands/asp/approve-adr.md` 後，harness 系統訊息實際將其註冊為 `asp:approve-adr`、`asp:review-work`（帶冒號）。
- **結論（已確認，針對本環境）**：
  1. `.claude/commands/<檔>.md`（直接位於 commands 下）→ 指令名 = 檔名去副檔名，例 `deploy.md` → `/deploy`。✅
  2. 冒號 namespace 文件主述為 plugin（`plugin-name:skill-name`）；但文件亦載「directory-qualified name」同樣用冒號（root `deploy` 與 `apps/web/.claude/skills/` 的 → 後者 `apps/web:deploy`）。
  3. **子目錄下的 command（`commands/asp/approve-adr.md`）在本環境實際註冊為 `asp:approve-adr`**（live 實證），故呼叫名為 `/asp:approve-adr`。審查員「冒號=plugin 限定」之推論被 live registration 推翻 → blocker 判為**誤報**。
- **殘留不確定**：使用者其他電腦若跑不同 Claude Code 版本，子目錄命名解析可能不同（最壞退化為 `/approve-adr`，無冒號）。緩解：**子目錄 `commands/asp/` 的檔案組織本身穩健**（安全整批同步的依據不變），僅「顯示/呼叫名是否帶冒號」會因版本而異。
- **再驗證條件**：Claude Code 若變更 `.claude/commands/` 子目錄的命名/namespacing 規則，FC-003 失效，須重審指令內文與 CHANGELOG 的呼叫名標示。

---

## FC-004 — Claude Code plugin / marketplace 機制（ADR-021 分發改造可行性）

- **日期**：2026-06-23
- **觸發**：ADR-021（採用官方 plugin marketplace 取代 29KB 自製 install.sh + 全域 sync）的可行性核心假設——ASP 強制力深綁 SessionStart + PreToolUse hook，須先證實官方 plugin 機制完整承載這些元件，否則改造會打掉護欄。
- **查證方式**：WebFetch 官方文件（`code.claude.com/docs/en/plugins`、`/plugins-reference`、`/plugin-marketplaces`，舊 `docs.anthropic.com` 已 301 遷移，全文存檔於 tool-results）。
- **結論（已確認）**：
  1. **Hook 事件**：`hooks/hooks.json`（plugin root）支援的事件含 **`SessionStart`**（plugins-reference L115「When a session begins or resumes」）與 **`PreToolUse`**（L119「Before a tool call executes. **Can block it**」）。→ ASP 的 L1（session-audit）與 L1.5（commit 閘）兩層強制力，plugin 機制原生承載。✅
  2. **腳本路徑**：`${CLAUDE_PLUGIN_ROOT}`（L635，plugin 安裝目錄絕對路徑）可在 hook command 引用 bundled 腳本。✅
  3. **元件打包**：plugin 可含 skills/、agents/、commands/、`hooks/hooks.json`、`.mcp.json`、`.lsp.json`、monitors/、bin/、settings.json；manifest = `.claude-plugin/plugin.json`（name/description/version/author）。✅
  4. **分發/安裝/更新**：`.claude-plugin/marketplace.json` 列 plugins → `/plugin marketplace add <owner/repo>`（GitHub 直接 host，含 private repo）；`version` 設則 bump 才更新，省略且走 git 則 **commit SHA = 版本**（每 commit 自動更新）；使用者 `/plugin marketplace update` 刷新。✅
  5. **官方/社群市集**：`claude-plugins-official`（Anthropic 策展）+ `claude-plugins-community`（`/plugin marketplace add anthropics/claude-plugins-community`，經審查）。✅
- **殘留不確定（兩點，列為 ADR-021 → Accepted 前的驗收條件）**：
  1. **hook 動態寫 `settings.local.json`**（ASP L2 動態 deny 的做法）官方文件未明載；但 plugin hook 以非沙箱 shell 執行（command 可任意 shell），技術上可寫檔。**需 POC 實證**。
  2. **plugin `settings.json` 目前只支援 `agent`/`subagentStatusLine` 兩鍵**（plugins 文件明載，未知鍵 silently ignored）→ ASP 動態 deny **不能**改走 plugin settings.json，必須留在 hook 端寫 settings.local.json（與殘留 1 同一驗收）。
  3. **skill 命名空間化**：plugin skill 一律 namespaced（`/plugin-name:skill`）→ ASP 現 `/asp <意圖>` router 改 plugin 後呼叫名會變（如 `/asp:...`），需評估對既有 docs/使用者肌肉記憶的影響（與報告 §2 mono-router 議題相關）。
- **整體判定**：核心承載（SessionStart + PreToolUse-deny + 打包 + 市集自動更新）**已證實，低風險高信心**；殘留集中在「動態 deny 的 hook 寫檔」與「命名空間遷移」兩點，皆可由 POC 收斂——故 ADR-021 為**低風險提案**而非探索性賭注。
- **再驗證條件**：Claude Code 若變更 plugin hook 事件集合、`${CLAUDE_PLUGIN_ROOT}`、marketplace.json schema 或 `/plugin marketplace add` 介面，FC-004 失效，須重審 ADR-021。

---

## FC-005 — addyosmani/agent-skills 結構與設計事實（深層借鏡報告依據）

- **日期**：2026-06-23
- **觸發**：深層借鏡研究報告（`docs/research/2026-06-23-addyosmani-agent-skills-deep-borrow.md`）對第三方 repo 的結構/設計宣稱須一手查證後方可寫入（外部事實驗證鐵則）。
- **查證方式**：WebFetch 一手原始檔——`raw.githubusercontent.com/addyosmani/agent-skills/main/README.md` 與 `/CONTRIBUTING.md`。
- **結論（已確認）**：
  1. **skill 數 = 24**：23 lifecycle + 1 meta `using-agent-skills`（後者「Maps incoming work to the right skill workflow and defines shared operating rules」）。✅
  2. **六階段 SDLC**：DEFINE / PLAN / BUILD / VERIFY / REVIEW / SHIP（+ Meta）。✅
  3. **設計原則（README 原文四條）**：「Process, not prose.」/「Anti-rationalization.」（每 skill 列藉口×反駁表）/「Verification is non-negotiable.」（「'Seems right' is never sufficient.」）/「Progressive disclosure.」（references「load only when needed, keeping token usage minimal.」）。✅
  4. **CONTRIBUTING 品質四標準**：Specific（"Actionable steps, not vague advice"）/ Verifiable（"Clear exit criteria with evidence requirements"）/ Battle-tested（"Based on real engineering workflows, not theoretical ideals"）/ Minimal（"Only the content needed to guide the agent correctly"）。✅
  5. **bundling 門檻**：「Don't create supporting files unless content exceeds 100 lines」——此 100 行為支援檔（scripts/references）分檔門檻，**非** PR 大小或單任務改檔上限（更正二手摘要誤植）。✅
  6. **frontmatter**：必填 `name` + `description`；description「starts with what the skill does (third person), then includes one or more `Use when` trigger conditions」。✅
  7. **marketplace 安裝**：`/plugin marketplace add addyosmani/agent-skills` + `/plugin install agent-skills@addy-agent-skills`。✅
  8. **slash 指令 8 個**：`/spec /plan /build /test /review /webperf /code-simplify /ship`。✅
  9. **授權**：MIT。✅
- **殘留不確定**：「每任務 ≤5 檔 = scope 過大警訊」此具體數字出自二手（部落格/個別 skill），README/CONTRIBUTING 一手未載 → 報告**不以一手事實斷言該數字**，僅借「把質性 scope 紀律量化為機械信號」之精神。star 數（≈65.7K）為動態統計非設計事實，僅作活躍度脈絡。
- **再驗證條件**：addyosmani/agent-skills 改版（skill 數/階段/原則/CONTRIBUTING 標準/marketplace 指令變動）時 FC-005 失效，須重審報告對應段落。

---

## FC-006 — Claude Code plugin hook 執行環境（ADR-021 POC-1 可行性）

- **日期**：2026-07-22
- **觸發**：ADR-021 POC-1——證實 plugin hook 能動態寫 `settings.local.json` 承載 ASP L2 動態 deny（FC-004 殘留 #1：官方文件未明載 hook 寫此檔）。
- **查證方式**：`claude-code-guide`（官方文件 `code.claude.com/docs`：plugins-reference / hooks / plugin-marketplaces / discover-plugins）。
- **結論（已確認，全數有文件佐證）**：
  1. **`CLAUDE_PROJECT_DIR` 對 plugin hook 有保證**——plugins-reference「Environment variables」列 `${CLAUDE_PLUGIN_ROOT}`/`${CLAUDE_PLUGIN_DATA}`/`${CLAUDE_PROJECT_DIR}` 提供給 hook，與 settings.json hook 相同。→ `session-audit.sh:18` `${CLAUDE_PROJECT_DIR:-.}` 在 plugin 下解析正確，deny 落對的專案。✅
  2. **hook stdin JSON 有 `cwd`（非 project dir）**——`cwd` 可能 ≠ project root；project dir **不在** stdin，須用 `CLAUDE_PROJECT_DIR` env（`pretooluse-ship-gate.sh:25` 已用 `.cwd` 作 fallback）。✅
  3. **`${CLAUDE_PLUGIN_ROOT}`**＝plugin 安裝目錄絕對路徑，hook command 可引用 bundled 腳本。**注意**：更新後路徑會變（舊版目錄約留兩週清），勿於其中存 state。✅
  4. **plugin hook 不沙箱**——以使用者 OS 權限跑；寫 `<project>/.claude/settings.local.json`／`docs/adr/` 允許。✅
  5. **manifest**：`marketplace.json` 需 `name`/`owner.name`/`plugins[]`（entry 需 `name`+`source`，local 用相對路徑 `"./subdir"`）；entry 設 `strict:false` 則**不需** plugin.json；hooks 放 `hooks/hooks.json`（`${CLAUDE_PLUGIN_ROOT}/hooks/x.sh`）。安裝：`/plugin marketplace add ./dir` → `/plugin install <plugin>@<marketplace>` → `/reload-plugins`。✅
  6. **local marketplace 支援**（`/plugin marketplace add ./path`）→ 就地測試、無需先推 GitHub。✅
- **本機實證**：`docs/research/poc1-plugin/simulate-plugin-hook.sh` 以 plugin env 跑 bundled `session-audit.sh` → deny 寫入 `<scratch>/.claude/settings.local.json`、tracked settings.json 不變（PASS）。詳 `docs/research/2026-07-22-poc1-plugin-hook-settings-local.md`。
- **結論對 ADR-021**：POC-1 核心（plugin hook 寫 settings.local.json 承載 L2 deny）＝**文件層級 YES + 本機模擬 PASS，且 ASP hook 無需修改**。session-audit 補對稱 stdin-`.cwd` fallback 為次要 robustness、非阻擋。
- **殘留（定論步，需人類跑一次 `/plugin`）**：真 `/plugin install` 下 Draft ADR → commit-deny 端到端觸發（步驟見 POC-1 報告）。因機制已文件確認，屬「確認」非「賭注」。
- **再驗證條件**：Claude Code 變更 plugin hook 事件集合／env 變數／stdin schema／marketplace.json schema／`/plugin` 介面時 FC-006 失效，須重審 ADR-021。

---

## FC-007 — SessionStart hook 的 stdin 含 `cwd`（ADR-029 / #73 關鍵前提）

- **日期**：2026-07-24
- **觸發**：#73 worktree-awareness 盤點 workflow 的 synthesis 斷言「**SessionStart hook 沒有 stdin `.cwd`** → session-audit 於 hook 內無法得知 active worktree → 需 Claude Code 平台改動」。此假設若錯，整個 ADR-029 的最大 blocker（🔴 platform-dependency）消失，改為 in-hook 可修。屬決定 ADR 走向的外部事實，必須查證。
- **查證方式**：官方文件 `code.claude.com/docs/en/hooks`（WebFetch，2026-07-24；原 `docs.claude.com/en/docs/claude-code/hooks` 301→`code.claude.com/docs/en/hooks`）。呼應 FC-006 point 2（`claude-code-guide` 已確認「hook stdin JSON 有 `cwd`」通則）。
- **結論（已確認，官方 schema 佐證）**：
  1. **SessionStart hook stdin JSON 明確含 `cwd`**——官方 SessionStart 範例 schema 逐字列出 `"cwd": "/Users/..."`（＋`session_id`/`transcript_path`/`hook_event_name`/`source`/選配 `model`）。`cwd` 定義＝「hook 被呼叫時的 current working directory」。✅
  2. `cwd` 為**通用輸入欄位**，跨 hook 事件皆送（非 PreToolUse 專屬）；ship-gate 用的正是此欄（`pretooluse-ship-gate.sh` `.cwd`）。✅
  3. **對 #73 的意義**：session-audit（SessionStart）**可**讀 stdin `.cwd` 取得使用者實際所在 worktree，套用與 #72 同樣的 `asp_resolve_worktree`（CLAUDE_PROJECT_DIR 為錨、同 superproject 才以 cwd worktree 覆蓋）。synthesis 的「SessionStart 死結」**不成立**；worktree 訊號**已由平台提供**，屬 in-hook 可修，非 🔴 平台待改。✅
- **殘留不確定（需一次 live 實證，非賭注）**：`cwd` 的**值**在「於 worktree 啟動 session」時是否＝該 worktree（而非被 chdir 到主樹）。文件定義為「hook 被呼叫時的 cwd」、且 ship-gate 的 PreToolUse `.cwd` 已實證為命令執行的 worktree（#72 commit eac1203 直接模擬佐證），強烈指向 SessionStart `.cwd` 亦＝啟動所在 worktree。定論步：worktree 內啟 session、令 SessionStart hook `echo stdin.cwd`。因機制已文件確認，屬「確認」非「賭注」。
- **再驗證條件**：Claude Code 變更 hook stdin schema（移除/改名 `cwd`）或 SessionStart 事件輸入欄位時 FC-007 失效，須重審 ADR-029 scope（尤其 🟢/🔴 分界）。

## FC-008 — pi coding agent 能力對應 ASP 四層強制力（ADR-028）

- **日期**：2026-07-22
- **觸發**：ADR-028（評估 pi 作為 ASP 替代 enforcement harness）——需確認 pi 對四層強制力各 primitive 的支援度。
- **查證方式**：本 session Workflow fan-out（6 維度：L1 / L1.5+L2 / L3 / L4 / profile-hook / auth）→ 每維度 web 研究 + 對抗式驗證（skeptic 二次查證），引 pi 官方 docs（`extensions.md` / `skills.md` / `prompt-templates.md` / `providers.md` / `models.md` / `sdk.md`、pi.dev、`examples/extensions/{permission-gate,subagent}`）。**校正註（2026-07-22）**：各維度信心**非齊一**——L1-sessionstart 維度的研究 agent 曾失敗、由 profile-hook 維度代覆蓋，該覆蓋把 `session_start` 與 `before_agent_start` 混為一談；第二次獨立 reality-check 直接查 `extensions.md` 原文更正之（見 claim #1）。故原「全數 CONFIRMED」不成立，L1 判定為事後更正版。
- **結論（已確認）**：
  1. **L1 SessionStart → native 事件、但須手寫「首輪 gate」邏輯（已更正）**：`extensions.md` 顯示 `session_start`（每 session 觸發一次，reason: started/loaded/reloaded）用途是 reestablish in-memory state、**無**注入訊息/改 system prompt 能力；能注入的是 `before_agent_start`（「Can inject a message and/or modify the system prompt」）但它**每個 user turn（prompt 提交後）觸發一次**、非每 session 一次；官方對 one-time startup 指向 async factory。→ 要複製 ASP「session 起始注入一次 briefing」須 `before_agent_start` + 首輪去重 gate（或 async factory 算 briefing + 首輪注入），**屬需手寫邏輯**、非乾淨薄綁定。⚠️（原判定「native、可注入」為 `session_start`/`before_agent_start` 混淆，已更正。）
  2. **L1.5 PreToolUse + L2 Dynamic Deny → native 機制、但無宣告式 deny-list config**：`pi.on("tool_call")` 回 `{block:true, reason}`（官方 `permission-gate.ts` 已示範擋 `rm -rf`）；deny 邏輯須寫成 TS handler（可讀外部檔模擬動態注入）。✅
  3. **L3 Skill Gates → native**：同一套 Agent Skills `SKILL.md`（pi 為 lenient 相容實作）；prompt templates = slash 命令。摩擦：`/skill:name` 叫用、prompt 目錄**非遞迴**（`/asp:*` 命名空間需壓平）、`$N` 為 1-based（ASP 慣用 `$ARGUMENTS` 可原樣）。✅
  4. **L4 Subagent QA → partial**：pi 明列「No sub-agents」為**非目標**，但有官方範例擴充 `examples/extensions/subagent/`（複製 `.claude/agents` 模型、子行程隔離、parallel max 8／4-concurrent、`tools` frontmatter 唯讀 + SDK `createReadOnlyTools()`）＋現成套件（`@vigolium/piolium` = security-auditor 對應、`@tintinweb/pi-subagents`）；隔離為**子行程級**非 in-process。✅
  5. **Profile/編譯 → native config**：憲法進 `SYSTEM.md`／`APPEND_SYSTEM.md`（pi 本就讀 `CLAUDE.md`）；~30 生命週期事件。僅 compiled-artifact + mtime 重編為非原生（擴充重建，影響小）。✅
  6. **合規 auth → native**：`ANTHROPIC_API_KEY`／`auth.json`／`--api-key`，與 `/login` OAuth 獨立；多供應商 via `models.json`。✅
- **對 ADR-028 的意涵（已更正）**：分三類——**無 native primitive、須另建＝L4**；**有 native primitive 但須手寫 handler 邏輯＝L1（`before_agent_start`+首輪 gate）、L1.5、L2（`tool_call` deny handler）**；**近原生薄綁定＝L3、Profile-config、auth**。仍屬中等 adapter 工作量（~2–3 擴充：briefing 注入[含首輪去重] + tool_call deny + L4 dispatch），非「重寫全部」；SOP 內容近原樣可攜。**原「四個 native、僅 L4 partial」為過度簡化，已更正**（L1 從薄綁定移入「需手寫邏輯」類）。
- **殘留不確定**：個別 event payload shape、`settings.json`/`auth.json` 完整 schema 未逐一 source 確認（docs 列舉層級）；L4 依賴社群/範例擴充成熟度；「API-key vs 訂閱無能力差異」為文件「未載差異」而非明文保證。
- **再驗證條件**：pi 變更擴充事件集合／skill/agent 規格／prompt-template 解析／auth 路線時 FC-008 失效，須重審 ADR-028 對應表。

---

## FC-009 — Anthropic 訂閱 OAuth 用於第三方 harness（pi）的條款狀態（ADR-028 合規前提）

- **日期**：2026-07-22
- **觸發**：使用者問「Claude Code Max 方案能否配 pi 用」；ADR-028 需釘死合規 auth 前提。
- **查證方式**：WebFetch 官方 Legal & Compliance（`code.claude.com/docs/en/legal-and-compliance`）+ 官方 Agent SDK 訂閱額度說明（`support.claude.com`）+ WebSearch 多來源（含 pi 官方 `pi-claude-auth` 套件頁自述）。
- **結論（已確認）**：
  1. Anthropic **2026-02 起明訂** OAuth（Free/Pro/Max）憑證僅限官方客戶端；第三方工具使用訂閱 OAuth = 違反 Consumer ToS，已有封號/斷用案例（OpenClaw/OpenCode）。✅
  2. pi 的 `pi-claude-auth` 擴充可讀 `~/.claude/.credentials.json`／macOS Keychain 的 Claude Code OAuth token（每 5 分鐘 re-sync、可 refresh）→ 技術上能用 Max 額度；但其自身說明標註為「community workaround、可能隨時失效、自負風險」。✅
  3. **「Agent SDK 訂閱額度」目前不存在**（**已更正**，2026-07-22 WebFetch 官方頁 `support.claude.com/en/articles/15036540` 原文確認）：曾於 2026-06-15 宣布的分離額度**已於生效前暫停**，官方明載「We're pausing the changes… For now, nothing has changed」「The previously announced monthly credit… isn't available」，Agent SDK／`claude -p`／第三方 app 用量**仍計入訂閱額度**。→ 就算該額度重啟，也**僅涉及建於 Claude Agent SDK 的工具，pi 非其一**；無論如何都不存在「用訂閱合規涵蓋 pi」的路徑。此點不獨立成立結論，載重理由回到 claim #1（OAuth 僅限官方客戶端）。✅
  4. 合規路線：pi 用 `ANTHROPIC_API_KEY` 按量付費（Console／Bedrock／Vertex）完全合規，但**不吃 Max 額度**。✅
- **對 ADR-028 的意涵**：pi 落地一律走 API-key（或他供應商），營運上禁 `/login` 訂閱 OAuth；「用 Max 額度跑 pi」**不列為合規選項**。最終結論由 claim #1（2026-02 OAuth 禁令）獨立支撐，不依賴 claim #3。
- **殘留不確定**：Anthropic 公開訊息曾反覆（2026-04 Boris Cherny 貼文後數日軟化、A/B 測試短暫全擋 Pro；2026-06 Agent SDK 額度宣布後又暫停）；條款為法律文件、非法律意見，最終以官方條款為準。
- **再驗證條件**：Anthropic OAuth 第三方使用條款變更、或**被暫停的 Agent SDK 分離額度計畫若重新上路**、或 `pi-claude-auth` 機制變動時 FC-009 失效，須重審 ADR-028 合規前提。

---

## FC-010 — maka 能力 + mattpocock/skills 結構與安裝狀態（ADR-030）

- **日期**：2026-07-24
- **觸發**：ADR-030（ASP 向 mattpocock/skills 逐 skill 借鏡 + 把 maka 納入 harness 地景）。
- **查證方式**：WebFetch maka raw README（`raw.githubusercontent.com/maka-agent/maka-agent/main/README.md`）+ 本 session 2 個 read-only Explore agent 讀 `~/.claude/plugins/marketplaces/mattpocock/skills/**/SKILL.md` + 本機安裝盤點（`~/.claude/plugins/`、`~/.agents/skills/`）。
- **結論（已確認）**：
  - **maka**：
    1. local-first agent workspace（Electron+TUI+CLI+headless、TypeScript）；**有 permission engine**——README 原文「File writes, Shell, and dangerous tool calls pass through the permission engine」（利 L1.5/L2 deny）。✅
    2. auth：**API key + 訂閱 OAuth**（Claude/Codex/GitHub Copilot/Cursor/Antigravity previews）；憑證存 plaintext `credentials.json`（POSIX 0700/0600）。✅
    3. 成熟度：「currently targets users running from source or contributing」「under active development」＝早期。✅
    4. **NOT STATED（一律標 unknown、不臆測）**：SKILL.md / Agent Skills 標準、subagents / sub-agent 編排、擴充可訂閱的 hook/lifecycle 事件、讀 `CLAUDE.md`、Claude Code 相容、**permission engine 的攔截時機（執行前 block vs 執行後補救——README 只說 tool calls「pass through the permission engine」，未言 timing）**——README 皆未載。⚠️
  - **mattpocock/skills**：
    5. **Claude Code plugin + Agent Skills 標準雙軌**（`npx skills@latest add mattpocock/skills` 或 `/plugin install mattpocock-skills@mattpocock`）。✅
    6. **user-invoked 編排 / model-invoked 紀律**分層；`grilling` = `grilling`（訪談迴圈）∘ `domain-modeling`（inline 書寫），`grill-me`/`grill-with-docs` 為薄別名。✅
    7. 已 **user-global 安裝兩份**（plugin `mattpocock-skills:*` v1.2.0 + 舊 loose copy `~/.agents/skills/` symlink，**版本漂移**）、**未 wire 進 ASP repo**、與 `asp-*` 命名空間不撞。✅
- **對 ADR-030 的意涵**：maka 於「內容=Agent Skills 可攜」比 pi **弱**（Agent Skills NOT STATED），但 permission engine 對 enforcement 是加分 → **watchlist**，成熟後再評完整逐層對應。mattpocock/skills 逐 skill 借用處置見 `docs/research/2026-07-24-mattpocock-skills-deep-borrow.md`。
- **殘留不確定**：maka 文件薄，多項 NOT STATED 標 unknown；star 數 README 未載（早期外部觀察 ~871 為動態統計、非設計事實，不斷言）。
- **再驗證條件**：maka 版本演進（skill/hook/subagent/Agent-Skills API 成形）、或 mattpocock/skills 改版（skill 增刪/重命名）時 FC-010 失效，須重審 ADR-030 與研究文件。

## FC-011 — git-guardrails-claude-code 一手行為（SPEC-016 借用基準，ADR-030）

- **日期**：2026-07-27
- **觸發**：SPEC-016 為 ADR-030 首個 POC 借用（`git-guardrails-claude-code`）。謂詞表與「借用處置＝BUILD-ASP-NATIVE」的論證，須以**一手來源實際行為**為基準（外部事實驗證鐵則），且須實證研究文件 L82 對 git-guardrails 的描述。FC-010 只查 mattpocock/skills 結構與安裝，未查此 skill 的行為。
- **查證方式**：本 session SPEC-016 謂詞稽核 Probe workflow（run `wf_3cd9da7b-23c`）之 read-only research agent 直接讀本機一手來源檔——`~/.claude/plugins/marketplaces/mattpocock/skills/misc/git-guardrails-claude-code/scripts/block-dangerous-git.sh`、同目錄 `SKILL.md`、`agents/openai.yaml`（`source_found: true`）。
- **結論（已確認）**：
  1. **機制**：PreToolUse hook。`jq -r '.tool_input.command'` 取整條 command → 對每個 pattern 跑 `grep -qE "$pattern"`——**未錨定（unanchored）substring 比對**，**無 argv tokenize、無引號分段**。命中 → **`exit 2` + stderr 訊息**（＝方式B）；未命中 → `exit 0`。**無明確的 jq-缺失處理分支**——且腳本無 `set -e`/`pipefail`，故 jq 缺失／JSON 畸形／`.tool_input.command` 缺 → `COMMAND` 取空（或字面 `"null"`）→ 全 grep 落空 → 落到結尾 `exit 0` **靜默放行**（＝隱式 **fail-OPEN**、放行危險命令，**非**報錯崩潰）。✅
  2. **blocked_operations（naive substring pattern 清單）**：`git push`、`push --force`、`git reset --hard`（含裸 `reset --hard`）、`git clean -f`、`git clean -fd`、`git branch -D`、`git checkout .`（regex）、`git restore .`（regex）。✅
  3. **BLOCKED 訊息（逐字）**：`BLOCKED: '$COMMAND' matches dangerous pattern '$pattern'. The user has prevented you from doing this.`（`$COMMAND`/`$pattern` 為 script 內 shell 變數佔位，非機密）。✅
  4. **研究文件 `docs/research/2026-07-24-mattpocock-skills-deep-borrow.md` L82 對 git-guardrails 的描述 = 與一手來源相符，無出入 → 驗證為真。** ✅
  5. **`agents/openai.yaml` 僅 `display_name`/`short_description`、無任何行為定義**——一手讀畢確認（非未知；openai adapter 不改變 hook 攔截行為）。✅
- **SPEC-016 對一手來源的差異（刻意偏離，均有理由；供審查者對照）**：
  - (a) **輸出方式**：原生方式B（`exit 2`+stderr）vs SPEC 方式A（`exit 0`+JSON `permissionDecision:deny`）——與 ASP ship-gate 樣板（SPEC-013）一致。
  - (b) **push 職責**：原生擋 push vs SPEC **defer**（交回既有 `denied-commands.json`，不重複職責）。
  - (c) **比對法**：原生未錨定 grep substring vs SPEC argv-token + 引號感知分段——修原生 substring 誤命中（如 `commit -m "…reset --hard…"` 會被原生誤擋，SPEC B2 案放行）。
  - (d) **失效模式**：原生為**靜默 fail-OPEN**（jq 缺／JSON 畸形 → `COMMAND` 取空 → `exit 0` 放行危險命令，無任何警示）vs SPEC 同為 fail-open 但帶 stderr WARN + defer（可見、可稽核）。兩者皆放行、皆不死鎖；差別在**原生無聲、SPEC 留痕**。
  - (e) **涵蓋面**：原生漏 stash clear/drop、`switch -C`/`-f`、`worktree remove -f`、`git rm -f`——SPEC 全補。（`git -C`/global-option **非**「漏擋操作」——原生 substring grep 對 `git -C /x reset --hard` 仍會命中「reset --hard」；SPEC 對其為 M0.4 tokenization 處理，防 global-option 位移吃掉子命令而非補漏。）
  - (f) **可稽核性**：原生無 escape/telemetry vs SPEC `ASP_GIT_OK=1` escape hatch + `GIT-GUARD` 遙測（可稽核 bypass）。
  - (g) **訊息口吻**：原生「The user has prevented you」vs SPEC ASP 鐵則口吻（CLAUDE-IR-1「破壞性前人類確認」）。
- **殘留不確定（3 項 unknown，誠實標記、不臆測）**：
  1. **harness 對 `exit 2` 的解讀未一手驗證**——`SKILL.md` 宣稱能擋，但未實測 Claude Code harness 收到 exit 2 的實際行為（block vs 忽略）。SPEC-016 亦於「誠實能力邊界」聲明本 hook 對 harness 攔截語義為設計假設、非實證。
  2. 原生 hook 的 **runtime false-positive/false-negative 未實測量化**（僅靜態讀 pattern 推得）。
  3. **無 shipped `hooks.json`/settings wiring**——僅 `SKILL.md` 散文描述安裝，未見實際掛載設定檔（安裝後行為未一手確認）。
- **對 SPEC-016 的意涵**：處置＝**BUILD-ASP-NATIVE**（依 ship-gate 樣板自寫、非 vendor 原 script）成立——原 script 淨價值集中在 (e) 補漏 + (f) 統一遙測/可稽核 bypass；其餘 substring 誤命中、**靜默 fail-OPEN（jq 缺即無聲放行危險命令）**、與既有層 push 職責重疊，皆為 SPEC 的修正點而非可直接複製之資產。
- **再驗證條件**：mattpocock/skills 改版（`block-dangerous-git.sh` 內容/pattern 變動、或機制由 `exit 2` 改 JSON）時 FC-011 失效；harness 對 PreToolUse exit-code 有一手文件時，補驗 unknown #1（並回填 SPEC-016 誠實能力邊界）。
